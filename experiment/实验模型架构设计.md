# 五种实验模型架构设计

本文档详细描述了屏幕检测器消融实验中的五种模型架构设计。

## 实验概览

| 实验 | 模型名称 | 描述 | 输入模式 |
|------|----------|------|----------|
| Exp0 | CNN Only | EfficientNet-B0 only | RGB |
| Exp1 | CNN+FFT | EfficientNet-B0 + FFT Branch | RGB + FFT |
| Exp2 | DeiT | DeiT-Small only | RGB |
| Exp3 | FFT+DeiT | Dual-stream DeiT | RGB + FFT |
| Exp4 | DWT+FFT+DeiT | Triple-stream DeiT | RGB + FFT + DWT |

---

## Exp0: CNN Only (EfficientNet-B0)

### 架构描述

最简单的基线模型，仅使用 EfficientNet-B0 处理 RGB 图像，不包含任何频域特征提取。

### 架构图

```mermaid
graph TD
    A[输入图像<br/>B, 3, 224, 224] --> B[EfficientNet-B0<br/>Backbone]
    B --> C[全局平均池化]
    C --> D[特征向量<br/>B, 1280]
    D --> E[Dropout 0.3]
    E --> F[Linear 1280→512]
    F --> G[ReLU]
    G --> H[Dropout 0.2]
    H --> I[Linear 512→3]
    I --> J[输出 logits<br/>B, 3]

    style A fill:#e1f5fe
    style J fill:#c8e6c9
```

### 关键参数

- **Backbone**: EfficientNet-B0 (ImageNet 预训练)
- **特征维度**: 1280
- **输出类别**: 3 (natural, screenshot, screen_photo)

---

## Exp1: CNN+FFT (EfficientNet-B0 + FFT Branch)

### 架构描述

双分支 CNN 架构，RGB 分支使用 EfficientNet-B0 提取空间特征，FFT 分支使用轻量 CNN 提取频域特征。

### 架构图

```mermaid
graph TD
    subgraph 输入
        A1[RGB 输入<br/>B, 3, 224, 224]
        A2[FFT 频谱图<br/>B, 1, 224, 224]
    end

    subgraph RGB分支
        B1[EfficientNet-B0<br/>Backbone]
        B2[LayerNorm]
        B3[空间特征<br/>B, 1280]
    end

    subgraph FFT分支
        C1[Conv2d 1→32<br/>3×3]
        C2[BatchNorm + ReLU]
        C3[ResBlock 32]
        C4[Conv2d 32→64<br/>3×3]
        C5[BatchNorm + ReLU]
        C6[ResBlock 64]
        C7[AdaptiveAvgPool<br/>4×4]
        C8[Flatten + Linear<br/>1024→256]
        C9[频域特征<br/>B, 256]
    end

    subgraph 融合
        D1[Concat<br/>B, 1536]
        D2[Dropout 0.3]
        D3[Linear 1536→512]
        D4[ReLU]
        D5[Dropout 0.2]
        D6[Linear 512→3]
    end

    A1 --> B1 --> B2 --> B3
    A2 --> C1 --> C2 --> C3 --> C4 --> C5 --> C6 --> C7 --> C8 --> C9

    B3 --> D1
    C9 --> D1
    D1 --> D2 --> D3 --> D4 --> D5 --> D6

    D6 --> E[输出 logits<br/>B, 3]

    style A1 fill:#e1f5fe
    style A2 fill:#fff3e0
    style E fill:#c8e6c9
```

### FFT Branch 详细结构

```mermaid
graph LR
    subgraph FFT Branch
        A[输入 1ch] --> B[Conv 32]
        B --> C[BN+ReLU]
        C --> D[ResBlock]
        D --> E[Conv 64]
        E --> F[BN+ReLU]
        F --> G[ResBlock]
        G --> H[Pool 4x4]
        H --> I[FC 256]
    end

    subgraph ResBlock
        R1[Conv 3×3] --> R2[BN+ReLU]
        R2 --> R3[Conv 3×3]
        R3 --> R4[BN]
        R4 --> R5[+ 残差]
        R5 --> R6[ReLU]
    end

    style A fill:#fff3e0
    style I fill:#fff3e0
```

### 关键参数

- **RGB Backbone**: EfficientNet-B0 (1280 维)
- **FFT Branch**: Conv→ResBlock→Conv→ResBlock→Pool→FC (256 维)
- **融合维度**: 1280 + 256 = 1536
- **输出类别**: 3

---

## Exp2: DeiT-Small

### 架构描述

纯 Vision Transformer 架构，使用 DeiT-Small (Data-efficient Image Transformer) 作为 backbone。

### 架构图

```mermaid
graph TD
    A[输入图像<br/>B, 3, 224, 224] --> B[Patch Embedding<br/>16×16 patches]
    B --> C[+ 位置编码<br/>+ CLS token]
    C --> D[Transformer Encoder<br/>×12 blocks]
    D --> E[CLS Token 提取]
    E --> F[特征向量<br/>B, 384]
    F --> G[Classification Head<br/>Linear 384→3]
    G --> H[输出 logits<br/>B, 3]

    subgraph Transformer Block
        T1[LayerNorm] --> T2[Multi-Head<br/>Self-Attention<br/>6 heads]
        T2 --> T3[+ 残差]
        T3 --> T4[LayerNorm]
        T4 --> T5[MLP<br/>384→1536→384]
        T5 --> T6[+ 残差]
    end

    style A fill:#e1f5fe
    style H fill:#c8e6c9
```

### 关键参数

- **Backbone**: DeiT-Small (deit_small_patch16_224)
- **Patch Size**: 16×16
- **Hidden Dim**: 384
- **Transformer Blocks**: 12
- **Attention Heads**: 6
- **参数量**: ~22M
- **输出类别**: 3

---

## Exp3: FFT+DeiT (Dual-stream)

### 架构描述

双流 Transformer 架构，RGB 和 FFT 各自通过独立的 DeiT-Small 提取特征，然后进行特征融合。

### 架构图

```mermaid
graph TD
    subgraph 输入
        A1[RGB 输入<br/>B, 3, 224, 224]
        A2[FFT 频谱图<br/>B, 1, 224, 224]
    end

    subgraph RGB流 - DeiT-Small
        B1[Patch Embedding]
        B2[Transformer Encoder<br/>×12 blocks]
        B3[CLS Token]
        B4[LayerNorm]
        B5[RGB 特征<br/>B, 384]
    end

    subgraph FFT流 - DeiT-Small
        C1[1ch→3ch 转换]
        C2[Patch Embedding]
        C3[Transformer Encoder<br/>×12 blocks]
        C4[CLS Token]
        C5[LayerNorm]
        C6[FFT 特征<br/>B, 384]
    end

    subgraph 融合分类器
        D1[Concat<br/>B, 768]
        D2[Dropout 0.3]
        D3[Linear 768→512]
        D4[GELU]
        D5[Dropout 0.2]
        D6[Linear 512→3]
    end

    A1 --> B1 --> B2 --> B3 --> B4 --> B5
    A2 --> C1 --> C2 --> C3 --> C4 --> C5 --> C6

    B5 --> D1
    C6 --> D1
    D1 --> D2 --> D3 --> D4 --> D5 --> D6

    D6 --> E[输出 logits<br/>B, 3]

    style A1 fill:#e1f5fe
    style A2 fill:#fff3e0
    style E fill:#c8e6c9
```

### 关键参数

- **RGB Stream**: DeiT-Small (384 维)
- **FFT Stream**: DeiT-Small (384 维)，FFT 1ch→3ch 重复
- **融合维度**: 384 + 384 = 768
- **分类器**: 768→512→3 (GELU 激活)
- **输出类别**: 3

---

## Exp4: DWT+FFT+DeiT (Triple-stream)

### 架构描述

三流融合架构，结合 RGB (DeiT)、FFT (DeiT) 和 DWT (CNN) 三个分支，充分利用空间、频域和小波域特征。

### 架构图

```mermaid
graph TD
    subgraph 输入
        A1[RGB 输入<br/>B, 3, 224, 224]
        A2[FFT 频谱图<br/>B, 1, 224, 224]
    end

    subgraph RGB流 - DeiT-Small
        B1[Patch Embedding]
        B2[Transformer Encoder<br/>×12 blocks]
        B3[CLS Token]
        B4[LayerNorm]
        B5[RGB 特征<br/>B, 384]
    end

    subgraph FFT流 - DeiT-Small
        C1[1ch→3ch 转换]
        C2[Patch Embedding]
        C3[Transformer Encoder<br/>×12 blocks]
        C4[CLS Token]
        C5[LayerNorm]
        C6[FFT 特征<br/>B, 384]
    end

    subgraph DWT流 - CNN
        D1[Haar DWT<br/>3ch→12ch]
        D2[Conv 64<br/>stride=2]
        D3[Conv 128<br/>stride=2]
        D4[Conv 256<br/>stride=2]
        D5[Conv 256<br/>stride=2]
        D6[AdaptiveAvgPool]
        D7[Linear 256→256]
        D8[LayerNorm]
        D9[DWT 特征<br/>B, 256]
    end

    subgraph 融合分类器
        E1[Concat<br/>B, 1024]
        E2[Dropout 0.3]
        E3[Linear 1024→512]
        E4[GELU]
        E5[Dropout 0.2]
        E6[Linear 512→3]
    end

    A1 --> B1 --> B2 --> B3 --> B4 --> B5
    A2 --> C1 --> C2 --> C3 --> C4 --> C5 --> C6
    A1 --> D1 --> D2 --> D3 --> D4 --> D5 --> D6 --> D7 --> D8 --> D9

    B5 --> E1
    C6 --> E1
    D9 --> E1
    E1 --> E2 --> E3 --> E4 --> E5 --> E6

    E6 --> F[输出 logits<br/>B, 3]

    style A1 fill:#e1f5fe
    style A2 fill:#fff3e0
    style F fill:#c8e6c9
```

### DWT Branch 详细结构

```mermaid
graph TD
    subgraph Haar DWT 分解
        A[RGB 输入<br/>B, 3, 224, 224] --> B[Haar 小波变换]
        B --> C1[LL 子带<br/>低频近似]
        B --> C2[LH 子带<br/>水平细节]
        B --> C3[HL 子带<br/>垂直细节]
        B --> C4[HH 子带<br/>对角细节]
        C1 --> D[拼接<br/>B, 12, 112, 112]
        C2 --> D
        C3 --> D
        C4 --> D
    end

    subgraph CNN 特征提取
        D --> E[Conv 64<br/>3×3, stride=2]
        E --> F[BN + GELU]
        F --> G[Conv 128<br/>3×3, stride=2]
        G --> H[BN + GELU]
        H --> I[Conv 256<br/>3×3, stride=2]
        I --> J[BN + GELU]
        J --> K[Conv 256<br/>3×3, stride=2]
        K --> L[BN + GELU]
        L --> M[AdaptiveAvgPool<br/>1×1]
        M --> N[Linear 256→256]
    end

    style A fill:#e1f5fe
    style N fill:#fff3e0
```

### DWT 子带说明

| 子带 | 名称 | 包含信息 | 对屏幕照片检测的作用 |
|------|------|----------|---------------------|
| LL | Low-Low | 低频近似 | 图像整体结构、边缘 |
| LH | Low-High | 水平细节 | 摩尔纹、水平条纹 |
| HL | High-Low | 垂直细节 | 屏幕边框、垂直线条 |
| HH | High-High | 对角细节 | 屏幕纹理、像素网格 |

### 关键参数

- **RGB Stream**: DeiT-Small (384 维)
- **FFT Stream**: DeiT-Small (384 维)
- **DWT Stream**: CNN (256 维)，Haar 小波 + 4 层 Conv
- **融合维度**: 384 + 384 + 256 = 1024
- **分类器**: 1024→512→3 (GELU 激活)
- **输出类别**: 3

---

## 模型对比总结

```mermaid
graph LR
    subgraph Exp0: CNN Only
        A1[RGB] --> B1[EfficientNet]
        B1 --> C1[1280d]
    end

    subgraph Exp1: CNN+FFT
        A2[RGB] --> B2[EfficientNet]
        A3[FFT] --> B3[CNN]
        B2 --> C2[1280d]
        B3 --> C3[256d]
    end

    subgraph Exp2: DeiT
        A4[RGB] --> B4[DeiT-Small]
        B4 --> C4[384d]
    end

    subgraph Exp3: FFT+DeiT
        A5[RGB] --> B5[DeiT-Small]
        A6[FFT] --> B6[DeiT-Small]
        B5 --> C5[384d]
        B6 --> C6[384d]
    end

    subgraph Exp4: DWT+FFT+DeiT
        A7[RGB] --> B7[DeiT-Small]
        A8[FFT] --> B8[DeiT-Small]
        A9[RGB] --> B9[DWT-CNN]
        B7 --> C7[384d]
        B8 --> C8[384d]
        B9 --> C9[256d]
    end

    style A1 fill:#e1f5fe
    style A2 fill:#e1f5fe
    style A4 fill:#e1f5fe
    style A5 fill:#e1f5fe
    style A7 fill:#e1f5fe
    style A9 fill:#e1f5fe
    style A3 fill:#fff3e0
    style A6 fill:#fff3e0
    style A8 fill:#fff3e0
```

## 技术要点

### 1. 特征归一化

所有模型在融合前都使用 LayerNorm 对各分支特征进行归一化：

```python
self.rgb_norm = nn.LayerNorm(feat_dim)
self.fft_norm = nn.LayerNorm(feat_dim)
self.dwt_norm = nn.LayerNorm(dwt_out_features)
```

### 2. FFT 输入处理

FFT 频谱图为单通道，送入 DeiT 前需转换为 3 通道：

```python
fft_3ch = fft_input.repeat(1, 3, 1, 1)  # (B, 1, H, W) -> (B, 3, H, W)
```

### 3. DWT 变换

使用 Haar 小波进行 2D 离散小波变换：

```python
# Haar 滤波器系数
lo = [1.0, 1.0] / 2.0  # 低通
hi = [1.0, -1.0] / 2.0  # 高通

# 2D 滤波器: 外积
ll = lo ⊗ lo  # Low-Low
lh = lo ⊗ hi  # Low-High
hl = hi ⊗ lo  # High-Low
hh = hi ⊗ hi  # High-High
```

### 4. 融合策略

所有多分支模型采用相同的融合策略：

1. **特征提取**: 各分支独立提取特征
2. **特征归一化**: LayerNorm 归一化
3. **特征拼接**: Concat 沿特征维度
4. **分类器**: Dropout → Linear → 激活 → Dropout → Linear

---

## 预期效果

| 实验 | 优势 | 劣势 |
|------|------|------|
| Exp0 | 简单快速，易于部署 | 缺乏频域信息 |
| Exp1 | 引入频域特征，参数量适中 | CNN 感受野有限 |
| Exp2 | 全局注意力，长距离依赖 | 参数量较大 |
| Exp3 | 双流互补，RGB+FFT | 计算量翻倍 |
| Exp4 | 三流融合，最全面 | 计算量最大，训练复杂 |
